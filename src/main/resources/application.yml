spring:
  application:
    name: thread-voyage-api

  datasource:
    url: jdbc:postgresql://localhost:5432/crochet_shop
    username: shop_admin
    password: super_secure_password_123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate 
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true